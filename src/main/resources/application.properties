spring.application.name=api
#spring.datasource.url=${DB_URL:jdbc:postgresql://localhost/postgres}
#spring.datasource.username=${DB_USER:fernandakipper}
#spring.datasource.password=${DB_PASSWORD:}
aws.region=${AWS_REGION:us-east-1}
aws.bucket.name=${AWS_BUCKET_NAME:eventostec-imagens}
#admin.key=${ADMIN_KEY:admin}
#aws.accessKey=
#aws.secretKey=
server.port=8080
server.servlet.context-path=/api


spring.datasource.url= jdbc:postgresql://localhost:5432/eventos_aws
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
